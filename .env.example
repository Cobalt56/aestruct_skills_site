# Database
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# Admin Authentication (Phase 1 - Simple password)
ADMIN_PASSWORD="admin123"

# Base URL
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# NextAuth (Phase 2)
AUTH_SECRET="" # Generate with: openssl rand -base64 32
AUTH_URL="http://localhost:3000"

# Stripe (Phase 2)
STRIPE_SECRET_KEY="sk_test_..." # Get from Stripe Dashboard
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..." # Get from Stripe Dashboard
STRIPE_WEBHOOK_SECRET="whsec_..." # Get from Stripe CLI or Dashboard

# Email (Phase 2)
EMAIL_PROVIDER="smtp" # "smtp" or "resend"
EMAIL_FROM="noreply@aestruct.com"

# Option 1: SMTP (Gmail, SendGrid, etc.)
EMAIL_SERVER_HOST="smtp.gmail.com"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER="your-email@gmail.com"
EMAIL_SERVER_PASSWORD="your-app-password"

# Option 2: Resend (Recommended)
RESEND_API_KEY="re_..." # Get from https://resend.com

# Download URL Security
DOWNLOAD_URL_SECRET="" # Use AUTH_SECRET if not set
